# Code RAG Assistant (VS Code)

## Prerequisites
- Backend running at `http://127.0.0.1:8000` with `/ping`, `/search`, `/explain`.
- Open this repository root as your VS Code workspace.

## Install & Run
```bash
cd clients/vscode
npm install
npm run build
